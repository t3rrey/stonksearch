import React from "react";
import Searchbar from "./Searchbar";

const Header = () => {
  return (
    <div id="header" className="h-64 w-full">
      <Searchbar />
    </div>
  );
};

export default Header;
